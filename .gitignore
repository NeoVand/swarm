node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Screen recordings - source footage for the README GIF, hundreds of MB each.
# The GIF built from them lives in static/; the recordings themselves do not
# belong in git history, where they could never be removed.
*.mov
*.mp4

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
